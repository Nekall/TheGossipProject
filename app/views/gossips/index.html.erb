<!DOCTYPE html>
<html lang="fr" dir="ltr">
  <head>
    <title>Accueil</title>
  </head>
  <body>
    <div class="jumbotron jumbotron-fluid">
      <div class="container">
        <h1 class="display-4">𝕋𝕙𝕖 𝔾𝕠𝕤𝕤𝕚𝕡 ℙ𝕣𝕠𝕛𝕖𝕔𝕥</h1>
        <p class="lead">Colporter sur le dos des gens, n'aurait jamais été aussi facile !</p>
      </div>
    </div>
    <div class="form-group">
      <a href="<%= new_gossip_path %>" class="btn btn-primary"> Créer un potin </a>
    </div>
    <h1>Liste des potins existants</h1>
    <% @gossips.each do |gossip|  %>
      <ul class="list-group">
        <li class="list-group-item">
          <h2> <%= gossip.title %> </h2>
          <p> <%= gossip.content %> </p>
          <p>
            <a href="<%= gossip_path(gossip.id) %>" class="btn btn-primary"> Détails </a>
            <a href="<%= edit_gossip_path(gossip.id) %>" class="btn btn-primary"> Editer </a>
            <a href="<%= gossip_path(gossip.id) %>" class="btn btn-danger" data-confirm="Confirmer la suppression du potin ?" data-method="DELETE"> Supprimer </a>
          </p>
        </li>
      </ul>
    <% end %>
  </body>
</html>